spring:
  application:
    name: vaier
  web:
    resources:
      static-locations: classpath:/static/
      cache:
        cachecontrol:
          no-cache: true
  mvc:
    static-path-pattern: /**

server:
  port: 8080
  address: 0.0.0.0

logging:
  level:
    net.vaier: DEBUG
    org.springframework: INFO

wireguard:
  server:
    endpoint: vaier.vaier.net:51820